const express = require('express');
const router = express.Router();

const mealController = require('../controller/mealController8');

router.post('/login', mealController.);

module.exports = router;
